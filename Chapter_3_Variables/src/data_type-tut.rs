fn understanding_data_types() {
    // scalar types - single value
    // Rust has 4 scalar type - int, float, numbers, floating point

    // Integer is a number without fractional component 
    // u is used for unsigned, i for signed
    // 8 bit - i8, u8; 16 bit - i16, u16; and so one 
    // arch - isize, usize
    // each variant - -(2^ n-1)  to 2^n-1
    




    
}

fn main() {
    understanding_data_types();
}